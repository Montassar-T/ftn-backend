<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="023-alter-users-role" author="Codex">
        <sql>ALTER TABLE users ADD COLUMN IF NOT EXISTS role VARCHAR(50) DEFAULT 'USER'</sql>
        <sql>UPDATE users SET role = 'ADMIN' WHERE id IN (SELECT id FROM users WHERE role IS NULL LIMIT 1)</sql>
        <sql>UPDATE users SET role = 'USER' WHERE role IS NULL</sql>
        <sql>ALTER TABLE users ALTER COLUMN role SET NOT NULL</sql>
        <sql>ALTER TABLE users ADD COLUMN IF NOT EXISTS phone VARCHAR(20)</sql>
        <sql>ALTER TABLE users ADD COLUMN IF NOT EXISTS photo VARCHAR(255)</sql>
    </changeSet>

</databaseChangeLog>
